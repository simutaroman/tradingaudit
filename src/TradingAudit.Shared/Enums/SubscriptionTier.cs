namespace TradingAudit.Shared.Enums;

public enum SubscriptionTier
{
    Free = 0,       // Початковий рівень (UserLevel0)
    Standard = 1,   // Платний базовий (UserLevel1)
    Pro = 2,        // Професійний (UserLevel2)
                    // Можна розширювати далі
}
